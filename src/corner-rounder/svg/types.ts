export type ToSvgElsOptions = {
  /**
   * The color used to draw the rounded-edge route.
   *
   * @default undefined // (SVG default is black)
   */
  color?: string
  /**
   * The width of the lines used to draw the rounded-edge route.
   *
   * @default undefined // (SVG default is 1)
   */
  lineWidth?: number
}
